# ══════════════════════════════════════
#  HydroBOS Environment Variables
# ══════════════════════════════════════
# Copy this file to .env and customize for your deployment.
# Docker Compose will automatically read .env from the project root.

# ── Security ──
JWT_SECRET=change-me-to-a-secure-random-string
JWT_EXPIRES_IN=24h

# ── Client URL ──
# The URL where the web client is accessible (used for CORS)
CLIENT_URL=http://localhost:3000

# ── Entra ID / Azure AD SSO (optional) ──
ENTRA_ENABLED=false
ENTRA_TENANT_ID=
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_REDIRECT_URI=http://localhost:5000/api/auth/callback
